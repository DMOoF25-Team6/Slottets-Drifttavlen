# Domain Class Diagram (DCD) for UC-005 Dashboard PhoneList

## Metadata
| Key            | Value                          |
|----------------|--------------------------------|
| Id             | UC-005.DCD                     |
| crossReference | UC-005.SD UC-005.SSD UC-005.OC |

## Version Log
| Version | Date       | Description                                           | Author |
|---------|------------|-------------------------------------------------------|--------|
| 0001    | 2026-04-04 | Initial DCD (WebApi controller depends on interface) | Team 6 |

---

## DCD for Domain Layer

```mermaid
classDiagram
  direction TB

  namespace Domain.Interfaces {
    class IEntity {
      <<interface>>
      +Id: Guid
    }
  }

  namespace Domain.Entities {
    class PhoneAssignment {
      +PhoneNumber: string
      +ShiftType: string
    }
  }

  PhoneAssignment --|> IEntity : implements
```

---

## DCD for Core Layer

```mermaid
classDiagram
  direction TB

  namespace Core.DTOs {
    class PhoneAssignmentDto {
      +PhoneNumber: string
      +ShiftType: string
    }
  }

  namespace Core.Interfaces.Managers {
    class IPhoneAssignmentManager {
      <<interface>>
      +GetCurrentPhoneAssignmentsForActiveShift(cancellationToken): Task~PhoneAssignmentDto[]~
    }
  }

  namespace Core.Services {
    class PhoneAssignmentService {
      -IPhoneAssignmentManager _phoneAssignmentManager
      +GetCurrentPhoneAssignmentsForActiveShift(cancellationToken): Task~PhoneAssignmentDto[]~
    }
  }

  PhoneAssignmentService --> IPhoneAssignmentManager : uses
  PhoneAssignmentDto ..> Domain.Entities.PhoneAssignment : represents
```

---

## DCD for Infrastructure Layer

```mermaid
classDiagram
  direction TB

  namespace Infrastructure.Managers {
    class PhoneAssignmentManager {
      +GetCurrentPhoneAssignmentsForActiveShift(cancellationToken): Task~PhoneAssignmentDto[]~
    }
  }

  Infrastructure.Managers.PhoneAssignmentManager ..|> Core.Interfaces.Managers.IPhoneAssignmentManager : implements
```

---

## DCD for WebApi Layer

```mermaid
classDiagram
  direction TB

  namespace WebApi.Controllers {
    class PhoneAssignmentsController {
      +GetCurrentPhoneAssignmentsForActiveShift(): ActionResult~PhoneAssignmentDto[]~
    }
  }

  WebApi.Controllers.PhoneAssignmentsController --> Core.Interfaces.Managers.IPhoneAssignmentManager : depends on
```

---

## Notes
- Clean Architecture dependency direction is preserved: WebApi depends on Core abstractions, and Infrastructure implements Core abstractions.
- The WebApi `PhoneAssignmentsController` must not reference `Infrastructure.Managers.PhoneAssignmentManager` directly.

## Language Handling
Professional English.
