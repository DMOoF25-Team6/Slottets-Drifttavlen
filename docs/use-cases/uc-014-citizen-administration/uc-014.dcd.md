# Domain Class Diagram (DCD) for UC-014: Administration - Create Citizen

## Metadata
| Key            | Value                         |
|----------------|-------------------------------|
| Id             | DCD-UC-014                    |
| crossReference | DM-UC-014, UC-014             |

## Version Log
| Version | Date       | Description                        | Author     |
|---------|------------|------------------------------------|------------|
| 0001    | 2026-05-03 | Initial for UC-014                 | Team 6     |

---

## DCD for UC-014 Domain Layer

```mermaid
classDiagram
  direction TB

  class Resident {
    +Id: Guid
    +Initials: string
    +FirstName: string
    +LastName: string
    +TrafficLightStatus: TrafficLightStatus
    +Notes: ICollection~ResidentNote~
    +Medicines: ICollection~MedicineRecord~
    +Painkillers: ICollection~PainkillerRecord~
  }
  class ResidentNote {
    +Id: Guid
    +Note: string
    +EditedAt: DateTime
  }
  class MedicineRecord {
    +Id: Guid
    +MedicineName: string
    +Timestamp: DateTime
    +Given: bool
  }
  class PainkillerRecord {
    +Id: Guid
    +Type: string
    +GivenAt: DateTime
    +NextAllowedTime: DateTime
  }
  class TrafficLightStatus {
    <<enumeration>>
    Green
    Yellow
    Red
  }

  Resident "1" --o "0..*" ResidentNote : has
  Resident "1" --o "0..*" MedicineRecord : has
  Resident "1" --o "0..*" PainkillerRecord : has
  Resident --* TrafficLightStatus : status
```

---

## DCD for UC-014 Application/Core Layer

```mermaid
classDiagram
  direction TB

  class ResidentService {
    -IResidentRepository _residentRepository
    -IResidentManager _residentManager
    +Create(request: ResidentRequestDto): IResidentResult
  }
  class ResidentRequestDto {
    +Initials: string
    +FirstName: string
    +LastName: string
    +TrafficLightStatus: TrafficLightStatus
  }
  class IResidentResult {
    <<interface>>
  }
  class ResidentResponseDto {
    +Id: Guid
    +Initials: string
    +FirstName: string
    +LastName: string
    +TrafficLightStatus: TrafficLightStatus
    +Notes: ICollection~ResidentNote~
    +Medicines: ICollection~MedicineRecord~
    +Painkillers: ICollection~PainkillerRecord~
  }
  class ErrorDto {
    +Error: string
    +ErrorCode: string
  }
  class IResidentRepository {
    <<interface>>
  }
  class IResidentManager {
    <<interface>>
    +CreateResident(request: ResidentRequestDto): IResidentResult
  }

  ResidentService --> IResidentRepository : uses
  ResidentService --> IResidentManager : uses
  ResidentService --> Resident : manages
  ResidentService --> ResidentRequestDto : receives
  ResidentService --> IResidentResult : returns
  ResidentResponseDto ..|> IResidentResult : implements
  ErrorDto ..|> IResidentResult : implements
```

---

## DCD for UC-014 Infrastructure Layer

```mermaid
classDiagram
  direction TB

  class ResidentManager {
    -HttpClient _httpClient
    +CreateResident(request: ResidentRequestDto): IResidentResult
  }
  class ResidentRepository {
    +AddAsync(entity: Resident): Resident
  }
  class ResidentApiController {
    +PostResident(request: ResidentRequestDto): IResidentResult
  }
  class InfrastructureData {
    +SaveResident(entity: Resident): Resident
  }

  ResidentManager --> Resident : manages
  ResidentManager --> ResidentRepository : uses
  ResidentApiController --> ResidentManager : uses
  ResidentRepository --> Resident : manages
  InfrastructureData --> Resident : persists
```

---

## DCD for UC-014 WebApi Layer

```mermaid
classDiagram
  direction TB

  class ResidentApiController {
    +PostResident(request: ResidentRequestDto): IResidentResult
  }
  ResidentApiController --> ResidentManager : uses
```

---

## DCD for UC-014 WebUI Layer

```mermaid
classDiagram
  direction TB

  class CitizenCreatePage {
    +EnterCitizenInfo(initials, firstName, lastName, status)
    +Submit()
    +ShowValidationError()
    +ShowDuplicateError()
    +ShowSuccess()
  }
  CitizenCreatePage --> ResidentRequestDto : creates
  CitizenCreatePage --> ResidentResponseDto : receives
  CitizenCreatePage --> ErrorDto : receives
```
