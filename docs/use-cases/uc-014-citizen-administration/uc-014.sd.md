## Metadata
| Key            | Value                                   |
|----------------|-----------------------------------------|
| Id             | UC-014.SD                              |
| crossReference | UC-014.SSD, DM-UC-014                  |

## Version Log
| Version | Date       | Description      | Author |
|---------|------------|------------------|--------|
| 0001    | 2026-05-03 | Initial SD       | Team 6 |

## Sequence Diagram

### Presentation Layer → Application Layer
```mermaid
sequenceDiagram
    participant WebUI
    participant ResidentService as Core.ResidentService
    WebUI->>ResidentService: Create(ResidentRequestDto)
    alt missingOrInvalidInformation
        ResidentService-->>WebUI: HighlightMissingOrInvalidFields()
    else duplicateCitizen
        ResidentService-->>WebUI: DisplayDuplicateCitizenError()
    else validInput
        ResidentService-->>WebUI: ResidentCreatedResponse(result)
    end
```

### Application Layer → Infrastructure Layer (Data Access)
```mermaid
sequenceDiagram
    participant ResidentService as Core.ResidentService
    participant Management as Infrastructure.ResidentManagement
    participant Api as Api.ResidentController
    participant Data as Infrastructure.Data
    ResidentService->>Management: CreateResident(ResidentRequestDto)
    alt missingOrInvalidInformation
        Management-->>ResidentService: HighlightMissingOrInvalidFields()
    else duplicateCitizen
        Management-->>ResidentService: DisplayDuplicateCitizenError()
    else validInput
        Management->>Api: PostResident(ResidentRequestDto)
        Api->>Data: SaveResident(ResidentRequestDto)
        Data-->>Api: ResidentSaved(IResidentResult)
        Api-->>Management: ResidentCreated(IResidentResult)
        Management-->>ResidentService: ResidentCreated(IResidentResult)
    end
```

**Notes:**
- All DTO transformations are handled at the boundaries between layers.
- No internal system calls or database details are shown beyond the Infrastructure.Data manager.
- Method names use PascalCase and parameters are shown in parentheses.
