# Domain Class Diagram (DCD) for Use Case 009: View History and Traceability

## Metadata
| Key               | Value                                                |
|-------------------|------------------------------------------------------|
| Id                | UC-009.DCD                                          |
| crossReference    | UC-009.SD<br>UC-009.OC<br>UC-009.DM                  |

## Version Log
| Version | Date       | Description              | Author     |
|---------|------------|--------------------------|------------|
| 0001    | 2026-05-08 | Initial                  | TEAM 6     |

---

```mermaid
%% DCD for Use Case 009: View History and Traceability
classDiagram
  direction TB

  namespace Core.Domain {
    class Overlap {
      +Guid Id
      +DateTime Start
      +DateTime End
      +string Shift
    }
    class AuditEntry {
      +Guid Id
      +DateTime EventTime
      +DateTime RegisteredTime
      +bool IsLateOrRetroactive
      +string ChangeDetails
      +Guid RecordId
    }
  }

  namespace Core.DTOs.HistoryTraceability {
    class OverlapQueryDto {
      +DateTime From
      +DateTime To
      +string Shift
    }
    class OverlapSummaryDto {
      +Guid Id
      +DateTime Start
      +DateTime End
      +string Shift
    }
    class OverlapSnapshotDto {
      +Guid Id
      +string Details
    }
    class AuditQueryDto {
      +Guid RecordId
      +DateTime? From
      +DateTime? To
      +string? Filters
    }
    class AuditEntrySummaryDto {
      +Guid Id
      +DateTime EventTime
      +DateTime RegisteredTime
      +bool IsLateOrRetroactive
    }
    class AuditEntryDetailsDto {
      +Guid Id
      +DateTime EventTime
      +DateTime RegisteredTime
      +bool IsLateOrRetroactive
      +string ChangeDetails
    }
  }

  namespace Core.Services {
    class HistoryTraceabilityService {
      +GetOverlapSummariesAsync(OverlapQueryDto): OverlapSummaryDto[]
      +GetOverlapSnapshotAsync(overlapId: Guid): OverlapSnapshotDto
      +GetAuditEntriesAsync(AuditQueryDto): AuditEntrySummaryDto[]
      +GetAuditEntryDetailsAsync(auditEntryId: Guid): AuditEntryDetailsDto
    }
  }

  namespace Infrastructure {
    class HistoryTraceabilityManager {
      +GetOverlapSummariesAsync(OverlapQueryDto): OverlapSummaryDto[]
      +GetOverlapSnapshotAsync(overlapId: Guid): OverlapSnapshotDto
      +GetAuditEntriesAsync(AuditQueryDto): AuditEntrySummaryDto[]
      +GetAuditEntryDetailsAsync(auditEntryId: Guid): AuditEntryDetailsDto
    }
  }

  namespace Infrastructure.Data {
    class OverlapRepository {
      +GetOverlapSummariesAsync(from, to, shift): OverlapSummaryDto[]
      +GetOverlapSnapshotAsync(overlapId): OverlapSnapshotDto
    }
    class AuditEntryRepository {
      +GetAuditEntriesAsync(query): AuditEntrySummaryDto[]
      +GetAuditEntryDetailsAsync(auditEntryId): AuditEntryDetailsDto
    }
  }

  %% Associations
  HistoryTraceabilityService --> OverlapQueryDto : uses
  HistoryTraceabilityService --> OverlapSummaryDto : returns
  HistoryTraceabilityService --> OverlapSnapshotDto : returns
  HistoryTraceabilityService --> AuditQueryDto : uses
  HistoryTraceabilityService --> AuditEntrySummaryDto : returns
  HistoryTraceabilityService --> AuditEntryDetailsDto : returns
  HistoryTraceabilityService --> HistoryTraceabilityManager : delegates
  HistoryTraceabilityManager --> OverlapRepository : uses
  HistoryTraceabilityManager --> AuditEntryRepository : uses
  OverlapRepository --> OverlapSummaryDto : returns
  OverlapRepository --> OverlapSnapshotDto : returns
  AuditEntryRepository --> AuditEntrySummaryDto : returns
  AuditEntryRepository --> AuditEntryDetailsDto : returns

  %% Notes
  %% - DTOs are used for all cross-layer boundaries
  %% - Service and manager classes orchestrate queries and data access
  %% - Domain entities are not exposed outside the domain
  %% - Clean Architecture dependency direction is preserved
```

---

## Notes
- DTOs are used for all cross-layer boundaries.
- Service and manager classes orchestrate queries and data access.
- Domain entities are not exposed outside the domain.
- Clean Architecture dependency direction is preserved.
- Late/retroactive entries are modeled with both `EventTime` and `RegisteredTime`.
- This DCD is based on the UC-009 sequence diagram and Clean Architecture conventions.
